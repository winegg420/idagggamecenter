node_modules
dist
.env
.env.local
.env.*.local
.vercel
supabase/.temp
supabase/.branches
.DS_Store
*.log

.vercel
.env*
!.env.example
# Sır içermez, yalnız derleme modunu seçer; izlenmeli ki
# `npm run build:bildim` temiz klonda da çalışsın.
!.env.bildim

# wrangler pages dev yerel önbelleği
.wrangler/

# Gecici calisma dosyalari (repoya girmez)
.tmp/

# Aşama 1E muayene: ham görünümler ara üründür (her çalıştırmada yeniden üretilir); kontakt sayfası + adaylar.json commit edilir
bildim/harita/muayene/cikti/**/*.png
